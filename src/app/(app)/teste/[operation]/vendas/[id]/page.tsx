import { notFound,redirect } from "next/navigation";
import { Badge } from "@/components/badge";
import { PageHeader } from "@/components/page-header";
import { TestLabSaleActions } from "@/components/test-lab-sale-actions";
import { getCurrentUserAccess,getTestLabSaleDetails } from "@/lib/data";
import { formatCurrency,formatDateOnly } from "@/lib/format";
import { parseTestLabOperation,testLabOperationLabel } from "@/lib/test-lab";
export default async function Page({params}:{params:Promise<{operation:string;id:string}>}){const{operation:raw,id}=await params;const operation=parseTestLabOperation(raw);if(!operation)notFound();const access=await getCurrentUserAccess();if(!access.canManageUsers)redirect("/dashboard");const sale=await getTestLabSaleDetails(id);if(!sale||sale.operation!==operation)notFound();return <><PageHeader eyebrow={`Área de Teste · ${testLabOperationLabel(operation)}`} title={sale.customer_name} description={`Venda fictícia de ${formatDateOnly(sale.quoted_on)}`}/><section className="sale-detail-summary-grid"><article className="panel"><div className="panel-body"><dl className="sale-detail-list"><div><dt>Pagamento</dt><dd><Badge value={sale.payment_status}/></dd></div><div><dt>Entrega</dt><dd><Badge value={sale.delivery_status}/></dd></div><div><dt>Reserva</dt><dd>{sale.reservation_status?<Badge value={sale.reservation_status} reservationContext="commercial"/> : "—"}</dd></div><div><dt>Total</dt><dd>{formatCurrency(sale.total_amount)}</dd></div><div><dt>Lucro</dt><dd>{formatCurrency(sale.total_profit)}</dd></div></dl></div></article></section><article className="panel"><div className="panel-head"><div><h2>Itens fictícios</h2></div></div><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Qtd.</th><th>Reservado</th><th>Preço</th></tr></thead><tbody>{sale.items.map((item: (typeof sale.items)[number])=><tr key={item.id}><td>{item.product_name}{item.variant_label?<small>{item.variant_label}</small>:null}</td><td>{item.quantity}</td><td>{item.quantity_reserved}</td><td>{formatCurrency(item.unit_price)}</td></tr>)}</tbody></table></div></article><article className="panel"><div className="panel-head"><div><h2>Ações de teste</h2><p>Execute a sequência que deseja validar.</p></div></div><div className="panel-body"><TestLabSaleActions saleId={sale.id} generalStatus={sale.general_status} paymentStatus={sale.payment_status} deliveryStatus={sale.delivery_status}/></div></article></>}
