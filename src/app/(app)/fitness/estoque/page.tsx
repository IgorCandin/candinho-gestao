import { PageHeader } from "@/components/page-header";
import { FitnessConversionForm } from "@/components/fitness-conversion-form";
import { getCurrentUserAccess, getFitnessStock } from "@/lib/data";
import { formatCurrency } from "@/lib/format";
export default async function Page(){
  const [access,stock]=await Promise.all([getCurrentUserAccess(),getFitnessStock()]); const salesMode=access.role==="sales";
  return <><PageHeader eyebrow="Candinho Fitness" title="Estoque" description={salesMode?"Consulta de disponibilidade e produtos a caminho.":"Saldo físico, reservado, disponível, a caminho e necessidade de reposição."}/>{salesMode&&<div className="sales-profile-note"><strong>Perfil Vendas</strong><span>Custos e ações de ajuste estão ocultos.</span></div>}<article className="panel"><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Tamanho</th><th>Cor</th><th>Físico</th><th>Reservado</th><th>Disponível</th><th>A caminho</th><th>Mínimo</th>{!salesMode&&<th>Custo</th>}<th>Status</th>{!salesMode&&<th>Ação</th>}</tr></thead><tbody>{stock.map((v)=><tr key={v.variant_id}><td>{v.product_name}</td><td>{v.size}</td><td>{v.color}</td><td>{v.physical_quantity}</td><td>{v.reserved_quantity}</td><td>{v.available_quantity}</td><td>{v.incoming_quantity}</td><td>{v.minimum_stock}</td>{!salesMode&&<td>{formatCurrency(v.stock_cost_value)}</td>}<td>{v.operational_status}</td>{!salesMode&&<td><a className="table-link" href={`/fitness/estoque/${v.variant_id}`}>Ajustar</a></td>}</tr>)}{stock.length===0&&<tr><td colSpan={salesMode?9:11}>Nenhuma variação cadastrada.</td></tr>}</tbody></table></div></article>{!salesMode&&access.canWriteFitness&&<FitnessConversionForm stock={stock}/>}</>;
}
