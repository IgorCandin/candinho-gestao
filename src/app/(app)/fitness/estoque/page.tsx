import Link from "next/link";import { redirect } from "next/navigation";import { Badge } from "@/components/badge";import { PageHeader } from "@/components/page-header";import { getCurrentUserAccess,getFitnessStock } from "@/lib/data";import { formatCurrency } from "@/lib/format";
export default async function Page(){const access=await getCurrentUserAccess();if(!access.canAccessFitness)redirect("/dashboard");const stock=await getFitnessStock();return <><PageHeader eyebrow="Candinho Fitness" title="Estoque" description="Controle por produto, tamanho e cor, com mínimo, ideal e reposição sugerida."/><article className="panel"><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Tam.</th><th>Cor</th><th>Físico</th><th>Reservado</th><th>Disponível</th><th>Mín.</th><th>Ideal</th><th>A caminho</th><th>Repor</th><th>Fornecedor</th><th>Venda</th><th>Status</th></tr></thead><tbody>{stock.map((row)=><tr key={row.variant_id}><td><Link className="cell-main dashboard-inline-link" href={`/fitness/produtos/${row.product_id}`}>{row.product_name}</Link></td><td>{row.size}</td><td>{row.color}</td><td>{row.physical_quantity}</td><td>{row.reserved_quantity}</td><td>{row.available_quantity}</td><td>{row.minimum_stock}</td><td>{row.reorder_target}</td><td>{row.incoming_quantity}</td><td>{row.suggested_reorder_quantity}</td><td>{row.default_supplier_name??"—"}</td><td className="amount">{formatCurrency(row.sale_price)}</td><td><Badge value={row.operational_status}/></td></tr>)}</tbody></table></div></article></>}
