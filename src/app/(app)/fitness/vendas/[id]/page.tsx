import Link from "next/link";import { notFound } from "next/navigation";import { Badge } from "@/components/badge";import { FitnessSaleActions } from "@/components/fitness-sale-actions";import { PageHeader } from "@/components/page-header";import { EntitySwipeNavigator } from "@/components/entity-swipe-navigator";import { getEntitySwipeNavigation,getFitnessSaleDetails } from "@/lib/data";import { formatCurrency,formatDateOnly } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const[sale,swipe]=await Promise.all([getFitnessSaleDetails(id),getEntitySwipeNavigation("fitness_sale",id)]);if(!sale)notFound();return <><PageHeader eyebrow="Candinho Fitness · Venda" title={sale.customer_name} description={`${formatDateOnly(sale.quoted_on)} · ${sale.product_summary}`} action={<Link className="button ghost" href="/fitness/vendas">Voltar às vendas</Link>}/><EntitySwipeNavigator previous={swipe.previous} next={swipe.next}/><section className="fitness-sale-status-grid"><article><span>Pagamento</span><strong><Badge value={sale.payment_status}/></strong><small>{sale.payment_method||"Forma não informada"}</small></article><article><span>Entrega</span><strong><Badge value={sale.delivery_status}/></strong><small>{sale.status_label}</small></article><article><span>Total da venda</span><strong>{formatCurrency(sale.total_amount)}</strong><small>Lucro {formatCurrency(sale.total_profit)}</small></article></section><div className="fitness-sale-operational-grid"><article className="panel"><div className="panel-head"><div><h2>Produtos da venda</h2><p>Tamanho, cor, quantidade, reserva e preço em um só lugar.</p></div></div><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Tamanho</th><th>Cor</th><th>Qtd.</th><th>Reservado</th><th>Preço</th></tr></thead><tbody>{sale.items.map((item)=><tr key={item.id}><td>{item.product_name}</td><td>{item.size}</td><td>{item.color}</td><td>{item.quantity}</td><td>{item.quantity_reserved}</td><td>{formatCurrency(item.unit_price)}</td></tr>)}</tbody></table></div></article><article className="panel"><div className="panel-head"><div><h2>Atualizar venda</h2><p>Registre recebimento, entrega ou cancelamento sem procurar outra tela.</p></div></div><div className="panel-body"><FitnessSaleActions saleId={sale.id} generalStatus={sale.general_status} paymentStatus={sale.payment_status} deliveryStatus={sale.delivery_status}/></div></article></div><article className="panel"><div className="panel-head"><div><h2>Informações</h2><p>Dados complementares da venda.</p></div></div><div className="panel-body"><dl className="details-list"><div><dt>Responsável</dt><dd>{sale.responsible||"—"}</dd></div><div><dt>Telefone</dt><dd>{sale.customer_phone||"—"}</dd></div><div><dt>Data do orçamento</dt><dd>{formatDateOnly(sale.quoted_on)}</dd></div><div><dt>Situação</dt><dd>{sale.status_label}</dd></div></dl></div></article></>}
