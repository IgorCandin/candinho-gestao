import { notFound,redirect } from "next/navigation";
import { Badge } from "@/components/badge";
import { FitnessSaleActions } from "@/components/fitness-sale-actions";
import { PageHeader } from "@/components/page-header";
import { getCurrentUserAccess,getFitnessSaleDetails } from "@/lib/data";
import { formatCurrency,formatDateOnly } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const access=await getCurrentUserAccess();if(!access.canAccessFitness)redirect("/dashboard");const {id}=await params;const sale=await getFitnessSaleDetails(id);if(!sale)notFound();return <><PageHeader eyebrow="Candinho Fitness" title={sale.customer_name} description={`Venda de ${formatDateOnly(sale.quoted_on)}`}/><section className="sale-detail-summary-grid"><article className="panel"><div className="panel-body"><dl className="sale-detail-list"><div><dt>Cliente</dt><dd>{sale.customer_name}</dd></div>{sale.customer_phone&&<div><dt>Telefone</dt><dd>{sale.customer_phone}</dd></div>}{sale.city&&<div><dt>Cidade</dt><dd>{sale.city}</dd></div>}<div><dt>Pagamento</dt><dd>{sale.paid_on?formatDateOnly(sale.paid_on):<Badge value={sale.payment_status}/>}</dd></div><div><dt>Entrega</dt><dd>{sale.delivered_on?formatDateOnly(sale.delivered_on):<Badge value={sale.delivery_status}/>}</dd></div><div><dt>Total</dt><dd>{formatCurrency(sale.total_amount)}</dd></div>{sale.notes&&<div><dt>Observações</dt><dd>{sale.notes}</dd></div>}</dl></div></article></section><article className="panel"><div className="panel-head"><div><h2>Peças</h2></div></div><div className="table-wrap"><table><thead><tr><th>Peça</th><th>Tamanho</th><th>Cor</th><th>Qtd.</th><th>Preço</th><th>Reserva</th></tr></thead><tbody>{sale.items.map((item)=><tr key={item.id}><td>{item.product_name}</td><td>{item.size}</td><td>{item.color}</td><td>{item.quantity}</td><td>{formatCurrency(item.unit_price)}</td><td>{item.reservation_status?<Badge value={item.reservation_status}/>:"—"}</td></tr>)}</tbody></table></div></article>{access.canWriteFitness&&<FitnessSaleActions saleId={sale.id} generalStatus={sale.general_status} paymentStatus={sale.payment_status} deliveryStatus={sale.delivery_status}/>}</>}
