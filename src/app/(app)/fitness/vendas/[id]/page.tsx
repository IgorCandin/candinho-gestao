import Link from "next/link";
import { notFound } from "next/navigation";
import { Badge } from "@/components/badge";
import { FitnessSaleActions } from "@/components/fitness-sale-actions";
import { PageHeader } from "@/components/page-header";
import { getFitnessSaleDetails } from "@/lib/data";
import { formatCurrency, formatDateOnly } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const sale=await getFitnessSaleDetails(id);if(!sale)notFound();return <><PageHeader eyebrow="Candinho Fitness · Venda" title={sale.customer_name} description={`${formatDateOnly(sale.quoted_on)} · ${sale.product_summary}`} action={<Link className="button ghost" href="/fitness/vendas">Voltar às vendas</Link>}/><section className="dashboard-two-column"><article className="panel"><div className="panel-head"><div><h2>Resumo</h2><p>{sale.status_label}</p></div></div><div className="panel-body"><dl className="details-list"><div><dt>Total</dt><dd>{formatCurrency(sale.total_amount)}</dd></div><div><dt>Lucro</dt><dd>{formatCurrency(sale.total_profit)}</dd></div><div><dt>Pagamento</dt><dd><Badge value={sale.payment_status}/></dd></div><div><dt>Entrega</dt><dd><Badge value={sale.delivery_status}/></dd></div><div><dt>Responsável</dt><dd>{sale.responsible||"—"}</dd></div><div><dt>Telefone</dt><dd>{sale.customer_phone||"—"}</dd></div></dl></div></article><article className="panel"><div className="panel-head"><div><h2>Ações</h2><p>Receber, entregar ou cancelar.</p></div></div><div className="panel-body"><FitnessSaleActions saleId={sale.id} generalStatus={sale.general_status} paymentStatus={sale.payment_status} deliveryStatus={sale.delivery_status}/></div></article></section><article className="panel"><div className="panel-head"><div><h2>Itens</h2><p>Peças vinculadas a esta venda.</p></div></div><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Tamanho</th><th>Cor</th><th>Qtd.</th><th>Reservado</th><th>Preço</th></tr></thead><tbody>{sale.items.map((item)=><tr key={item.id}><td>{item.product_name}</td><td>{item.size}</td><td>{item.color}</td><td>{item.quantity}</td><td>{item.quantity_reserved}</td><td>{formatCurrency(item.unit_price)}</td></tr>)}</tbody></table></div></article></>}
