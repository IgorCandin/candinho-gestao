import Link from "next/link";
import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { getFitnessProduct } from "@/lib/data";
import { formatCurrency } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const data=await getFitnessProduct(id);if(!data)notFound();const{product,variants}=data;return <><PageHeader eyebrow="Candinho Fitness · Produto" title={product.name} description={product.description||product.category} action={<Link className="button gold" href={`/fitness/produtos/${id}/editar`}>Editar produto</Link>}/><section className="stats-grid"><div className="stat-card"><span>Disponível</span><strong>{product.available_quantity}</strong><small>{product.reserved_quantity} reservado(s)</small></div><div className="stat-card"><span>A caminho</span><strong>{product.incoming_quantity}</strong><small>Reposições pendentes</small></div><div className="stat-card"><span>Variações</span><strong>{product.variant_count}</strong><small>{product.attention_variants} pedindo atenção</small></div></section><article className="panel"><div className="table-wrap"><table><thead><tr><th>Tamanho</th><th>Cor</th><th>SKU</th><th>Físico</th><th>Reservado</th><th>Disponível</th><th>A caminho</th><th>Custo</th><th>Venda</th></tr></thead><tbody>{variants.map((v)=><tr key={v.variant_id}><td>{v.size}</td><td>{v.color}</td><td>{v.sku||"—"}</td><td>{v.physical_quantity}</td><td>{v.reserved_quantity}</td><td>{v.available_quantity}</td><td>{v.incoming_quantity}</td><td>{formatCurrency(v.cost_price)}</td><td>{formatCurrency(v.sale_price)}</td></tr>)}</tbody></table></div></article></>}
