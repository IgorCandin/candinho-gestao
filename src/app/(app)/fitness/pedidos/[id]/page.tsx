import Link from "next/link";
import { notFound } from "next/navigation";
import { FitnessReceiveItemForm } from "@/components/fitness-receive-item-form";
import { FitnessReceiveOrderForm } from "@/components/fitness-receive-order-form";
import { PageHeader } from "@/components/page-header";
import { getFitnessPurchaseOrderDetails } from "@/lib/data";
import { formatCurrency, formatDateOnly } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const order=await getFitnessPurchaseOrderDetails(id);if(!order)notFound();return <><PageHeader eyebrow="Candinho Fitness · Pedido" title={order.supplier_name} description={`${formatDateOnly(order.ordered_on)} · ${order.product_summary}`} action={<Link className="button ghost" href="/fitness/pedidos">Voltar aos pedidos</Link>}/><section className="stats-grid"><div className="stat-card"><span>Pedido</span><strong>{order.ordered_units}</strong><small>unidades</small></div><div className="stat-card"><span>Recebido</span><strong>{order.received_units}</strong><small>{order.pending_units} pendente(s)</small></div><div className="stat-card"><span>Total</span><strong>{formatCurrency(order.grand_total)}</strong><small>{formatCurrency(order.freight)} de frete</small></div></section>{order.pending_units>0&&<article className="panel"><div className="panel-head"><div><h2>Receber pedido</h2><p>Receba tudo de uma vez ou item por item abaixo.</p></div></div><div className="panel-body"><FitnessReceiveOrderForm orderId={order.id}/></div></article>}<article className="panel"><div className="table-wrap"><table><thead><tr><th>Produto</th><th>Tamanho</th><th>Cor</th><th>Pedido</th><th>Recebido</th><th>Pendente</th><th>Custo</th><th>Ação</th></tr></thead><tbody>{order.items.map((item)=><tr key={item.id}><td>{item.product_name}</td><td>{item.size}</td><td>{item.color}</td><td>{item.quantity_ordered}</td><td>{item.quantity_received}</td><td>{item.quantity_pending}</td><td>{formatCurrency(item.total_cost)}</td><td>{item.quantity_pending>0?<FitnessReceiveItemForm itemId={item.id} pending={item.quantity_pending}/>:"Recebido"}</td></tr>)}</tbody></table></div></article></>}
