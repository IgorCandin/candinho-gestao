import { notFound,redirect } from "next/navigation";
import { Badge } from "@/components/badge";
import { FitnessReceiveItemForm } from "@/components/fitness-receive-item-form";
import { PageHeader } from "@/components/page-header";
import { getCurrentUserAccess,getFitnessPurchaseOrderDetails } from "@/lib/data";
import { formatCurrency,formatDateOnly } from "@/lib/format";
export default async function Page({params}:{params:Promise<{id:string}>}){const access=await getCurrentUserAccess();if(!access.canAccessFitness)redirect("/dashboard");const {id}=await params;const order=await getFitnessPurchaseOrderDetails(id);if(!order)notFound();return <><PageHeader eyebrow="Candinho Fitness" title={order.supplier_name} description={`Pedido de ${formatDateOnly(order.ordered_on)} · ${order.pending_units} peça(s) pendente(s)`}/><article className="panel"><div className="panel-body"><dl className="sale-detail-list"><div><dt>Status</dt><dd><Badge value={order.status}/></dd></div><div><dt>Total</dt><dd>{formatCurrency(order.order_total)}</dd></div><div><dt>Pedido</dt><dd>{order.ordered_units}</dd></div><div><dt>Recebido</dt><dd>{order.received_units}</dd></div>{order.notes&&<div><dt>Observações</dt><dd>{order.notes}</dd></div>}</dl></div></article><article className="panel"><div className="panel-head"><div><h2>Itens</h2><p>Receba cada variação separadamente.</p></div></div><div className="table-wrap"><table><thead><tr><th>Peça</th><th>Tamanho</th><th>Cor</th><th>Pedido</th><th>Recebido</th><th>Pendente</th><th>Custo</th><th>Ação</th></tr></thead><tbody>{order.items.map((item)=><tr key={item.id}><td>{item.product_name}</td><td>{item.size}</td><td>{item.color}</td><td>{item.quantity_ordered}</td><td>{item.quantity_received}</td><td>{item.quantity_pending}</td><td>{formatCurrency(item.unit_cost)}</td><td>{access.canWriteFitness&&item.quantity_pending>0?<FitnessReceiveItemForm itemId={item.id} pending={item.quantity_pending}/>:<Badge value={item.item_status}/>}</td></tr>)}</tbody></table></div></article></>}
