"use client";
import { LoaderCircle, PackageCheck } from "lucide-react";import { useState } from "react";import { useRouter } from "next/navigation";import { createClient } from "@/lib/supabase/client";
const today=()=>new Intl.DateTimeFormat("en-CA",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());
export function FitnessReceiveOrderForm({orderId}:{orderId:string}){const router=useRouter();const [date,setDate]=useState(today);const [loading,setLoading]=useState(false);const [message,setMessage]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage(null);try{const {error}=await createClient().rpc("receive_fitness_purchase_order",{p_order_id:orderId,p_received_on:date,p_notes:"Recebimento completo do pedido"});if(error)throw error;router.refresh()}catch(e){setMessage(e instanceof Error?e.message:"Não foi possível receber o pedido.")}finally{setLoading(false)}}return <form className="inline-action-form" onSubmit={submit}><input className="input" type="date" value={date} onChange={(e)=>setDate(e.target.value)}/><button className="button gold" disabled={loading}>{loading?<LoaderCircle className="spin" size={16}/>:<PackageCheck size={16}/>}Receber tudo</button>{message&&<span className="form-error visible">{message}</span>}</form>}
