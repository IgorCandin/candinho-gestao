"use client";
import { useRouter } from "next/navigation";import { Badge } from "@/components/badge";import { formatCurrency,formatDateOnly } from "@/lib/format";
type Row={id:string;customer_name:string;city:string|null;product_summary:string|null;quoted_on:string;payment_status:string;delivery_status:string;reservation_status:string|null;total_amount:number};const label=(name:string,city:string|null)=>city?.trim()?`${name} - ${city}`:name;
export function FitnessSalesTable({sales}:{sales:Row[]}){const router=useRouter();return <div className="table-wrap"><table><thead><tr><th>Cliente</th><th>Produtos</th><th>Data</th><th>Pagamento</th><th>Entrega</th><th>Reserva</th><th>Total</th></tr></thead><tbody>{sales.map((s)=><tr key={s.id} className="clickable-data-row" role="link" tabIndex={0} onClick={()=>router.push(`/fitness/vendas/${s.id}`)} onKeyDown={(e)=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();router.push(`/fitness/vendas/${s.id}`)}}}><td><strong>{label(s.customer_name,s.city)}</strong><small className="crm-cell-note">Clique para abrir a venda</small></td><td>{s.product_summary}</td><td>{formatDateOnly(s.quoted_on)}</td><td><Badge value={s.payment_status}/></td><td><Badge value={s.delivery_status}/></td><td><Badge value={s.reservation_status}/></td><td>{formatCurrency(s.total_amount)}</td></tr>)}{sales.length===0&&<tr><td colSpan={7}>Nenhuma venda registrada.</td></tr>}</tbody></table></div>}
