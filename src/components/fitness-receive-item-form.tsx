"use client";
import { LoaderCircle, PackageCheck } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
const today=()=>new Intl.DateTimeFormat("en-CA",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());
export function FitnessReceiveItemForm({itemId,pending}:{itemId:string;pending:number}){const router=useRouter();const [quantity,setQuantity]=useState(String(pending));const [receivedOn,setReceivedOn]=useState(today);const [loading,setLoading]=useState(false);const [message,setMessage]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage(null);try{const {error}=await createClient().rpc("receive_fitness_purchase_item",{p_item_id:itemId,p_quantity:Number(quantity),p_received_on:receivedOn,p_notes:null});if(error)throw error;router.refresh();}catch(e){setMessage(e instanceof Error?e.message:"Não foi possível receber o item.");}finally{setLoading(false);}}return <form className="receive-item-form" onSubmit={submit}><label className="field"><span>Qtd.</span><input className="input" type="number" min="1" max={pending} value={quantity} onChange={(e)=>setQuantity(e.target.value)}/></label><label className="field"><span>Data</span><input className="input" type="date" value={receivedOn} onChange={(e)=>setReceivedOn(e.target.value)}/></label><button className="button gold" disabled={loading}>{loading?<LoaderCircle className="spin" size={16}/>:<PackageCheck size={16}/>}Receber</button>{message&&<p className="form-error visible">{message}</p>}</form>}
