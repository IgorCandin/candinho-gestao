"use client";
import { ArrowRightLeft, ClipboardCheck, LoaderCircle, PackagePlus, SlidersHorizontal, X } from "lucide-react";import { useEffect,useMemo,useState } from "react";import { useRouter } from "next/navigation";import { createClient } from "@/lib/supabase/client";import type { InventoryLocationRow,InventoryOverviewRow,LocationOption } from "@/lib/types";
type ActionMode="count"|"adjust"|"transfer"|null;type FlavorOption={id:string;productId:string;name:string};type FlavorInventory={flavorId:string;locationId:string;physical:number;reserved:number;available:number;incoming:number};function todayBrazil(){return new Intl.DateTimeFormat("en-CA",{timeZone:"America/Sao_Paulo",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date())}
export function InventoryActions({products,locations,locationRows,initialProductId=""}:{products:InventoryOverviewRow[];locations:LocationOption[];locationRows:InventoryLocationRow[];initialProductId?:string}){const router=useRouter();const defaultLocation=locations.find((l)=>l.code==="CS")?.id??locations[0]?.id??"";const[mode,setMode]=useState<ActionMode>(null);const[productId,setProductId]=useState(initialProductId);const[flavorId,setFlavorId]=useState("");const[locationId,setLocationId]=useState(defaultLocation);const[destinationId,setDestinationId]=useState(locations.find((l)=>l.id!==defaultLocation)?.id??"");const[quantity,setQuantity]=useState("");const[date,setDate]=useState(todayBrazil());const[notes,setNotes]=useState("");const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");const[flavors,setFlavors]=useState<FlavorOption[]>([]);const[flavorInventory,setFlavorInventory]=useState<FlavorInventory[]>([]);
useEffect(()=>{let cancelled=false;async function load(){const supabase=createClient();const[a,b]=await Promise.all([supabase.from("product_flavors").select("id,product_id,name").eq("active",true).order("display_order").order("name"),supabase.from("product_flavor_inventory_overview").select("flavor_id,location_id,physical_quantity,reserved_quantity,available_quantity,incoming_quantity")]);if(cancelled)return;const error=a.error||b.error;if(error){setMessage(error.message);return}setFlavors((a.data??[]).map((r)=>({id:String(r.id),productId:String(r.product_id),name:String(r.name??"")})));setFlavorInventory((b.data??[]).map((r)=>({flavorId:String(r.flavor_id),locationId:String(r.location_id),physical:Number(r.physical_quantity??0),reserved:Number(r.reserved_quantity??0),available:Number(r.available_quantity??0),incoming:Number(r.incoming_quantity??0)})))}void load();return()=>{cancelled=true}},[]);
const current=useMemo(()=>locationRows.find((r)=>r.product_id===productId&&r.location_id===locationId)??null,[locationRows,productId,locationId]);const selectedProduct=products.find((p)=>p.product_id===productId);const productFlavors=flavors.filter((f)=>f.productId===productId);const selectedFlavor=productFlavors.find((f)=>f.id===flavorId)??null;const selectedFlavorInventory=flavorInventory.find((r)=>r.flavorId===flavorId&&r.locationId===locationId)??null;function open(next:Exclude<ActionMode,null>){setMode(next);setMessage("");setQuantity("");setNotes("");setDate(todayBrazil());setFlavorId("");if(initialProductId)setProductId(initialProductId)}function close(){if(!loading)setMode(null)}
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("");try{if(!productId||!locationId)throw new Error("Selecione o produto e o local.");if(productFlavors.length>0&&!flavorId)throw new Error("Selecione o sabor.");const supabase=createClient();if(mode==="count"){const{error}=await supabase.rpc("register_inventory_count_v2",{p_product_id:productId,p_location_id:locationId,p_flavor_id:flavorId||null,p_counted_quantity:Number(quantity),p_counted_on:date,p_notes:notes.trim()||null});if(error)throw error}if(mode==="adjust"){const{error}=await supabase.rpc("register_inventory_adjustment_v2",{p_product_id:productId,p_location_id:locationId,p_flavor_id:flavorId||null,p_quantity_delta:Number(quantity),p_occurred_on:date,p_notes:notes.trim()||null});if(error)throw error}if(mode==="transfer"){if(!destinationId)throw new Error("Selecione o estoque de destino.");const{error}=await supabase.rpc("transfer_inventory_v2",{p_product_id:productId,p_source_location_id:locationId,p_destination_location_id:destinationId,p_flavor_id:flavorId||null,p_quantity:Number(quantity),p_transferred_on:date,p_notes:notes.trim()||null});if(error)throw error}setMode(null);router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Não foi possível atualizar o estoque.")}finally{setLoading(false)}}
return <><div className="inventory-action-buttons"><button className="button ghost" type="button" onClick={()=>open("count")} title="Faça uma conferência física completa do saldo de um produto"><ClipboardCheck size={16}/>Contagem física</button><button className="button ghost" type="button" onClick={()=>open("adjust")} title="Corrija uma diferença conhecida sem iniciar uma contagem completa"><SlidersHorizontal size={16}/>Correção rápida</button><button className="button gold" type="button" onClick={()=>open("transfer")}><ArrowRightLeft size={16}/>Transferir</button></div>{mode&&<div className="modal-backdrop" role="presentation" onMouseDown={(e)=>{if(e.target===e.currentTarget)close()}}><form className="inventory-modal" onSubmit={submit}><div className="inventory-modal-head"><div><span className="eyebrow">Estoque</span><h2>{mode==="count"?"Contagem física de estoque":mode==="adjust"?"Correção rápida de saldo":"Transferência"}</h2><p>{mode==="count"?"Use quando você contou fisicamente o que existe no local. O sistema registra a diferença entre o saldo anterior e a contagem real.":mode==="adjust"?"Use para uma diferença já conhecida, como avaria, perda ou unidade encontrada. Informe + para entrada e - para saída.":"Move apenas unidades disponíveis do sabor selecionado, sem mexer nas reservas."}</p></div><button className="icon-button" type="button" aria-label="Fechar" onClick={close}><X size={18}/></button></div><div className="inventory-modal-body"><label className="field"><span>Produto</span><select className="select" required value={productId} onChange={(e)=>{setProductId(e.target.value);setFlavorId("")}} disabled={Boolean(initialProductId)}><option value="">Selecione o produto</option>{products.map((p)=><option key={p.product_id} value={p.product_id}>{p.product_name}</option>)}</select></label>{productFlavors.length>0&&<label className="field"><span>Sabor</span><select className="select" required value={flavorId} onChange={(e)=>setFlavorId(e.target.value)}><option value="">Selecione o sabor</option>{productFlavors.map((f)=>{const row=flavorInventory.find((r)=>r.flavorId===f.id&&r.locationId===locationId);return <option key={f.id} value={f.id}>{f.name} · disp. {row?.available??0}</option>})}</select></label>}<label className="field"><span>{mode==="transfer"?"Estoque de origem":"Local"}</span><select className="select" required value={locationId} onChange={(e)=>setLocationId(e.target.value)}>{locations.map((l)=><option key={l.id} value={l.id}>{l.code} · {l.name}</option>)}</select></label>{mode==="transfer"&&<label className="field"><span>Estoque de destino</span><select className="select" required value={destinationId} onChange={(e)=>setDestinationId(e.target.value)}><option value="">Selecione o destino</option>{locations.filter((l)=>l.id!==locationId).map((l)=><option key={l.id} value={l.id}>{l.code} · {l.name}</option>)}</select></label>}<label className="field"><span>{mode==="count"?"Quantidade realmente contada":mode==="adjust"?"Variação conhecida do saldo":"Quantidade a transferir"}</span><input className="input" type="number" required min={mode==="count"?0:mode==="transfer"?1:undefined} step="1" value={quantity} onChange={(e)=>setQuantity(e.target.value)} placeholder={mode==="adjust"?"Ex.: 3 ou -2":"0"}/></label><label className="field"><span>Data da operação</span><input className="input" type="date" required value={date} onChange={(e)=>setDate(e.target.value)}/></label><label className="field field-span-two"><span>Observação</span><textarea className="textarea" rows={3} value={notes} onChange={(e)=>setNotes(e.target.value)} placeholder="Motivo, conferência, responsável..."/></label>{selectedProduct&&<div className="inventory-current-strip"><span>{selectedProduct.product_name}{selectedFlavor?` · ${selectedFlavor.name}`:""}</span><span>Físico <strong>{selectedFlavorInventory?.physical??current?.physical_quantity??0}</strong></span><span>Reservado <strong>{selectedFlavorInventory?.reserved??current?.reserved_quantity??0}</strong></span><span>Disponível <strong>{selectedFlavorInventory?.available??current?.available_quantity??0}</strong></span>{selectedFlavorInventory&&<span>A caminho <strong>{selectedFlavorInventory.incoming}</strong></span>}</div>}{message&&<p className="form-message">{message}</p>}</div><div className="inventory-modal-actions"><button className="button ghost" type="button" onClick={close}>Cancelar</button><button className="button gold" type="submit" disabled={loading}>{loading?<LoaderCircle className="spin" size={16}/>:mode==="transfer"?<ArrowRightLeft size={16}/>:mode==="count"?<ClipboardCheck size={16}/>:<PackagePlus size={16}/>} {loading?"Salvando":"Confirmar"}</button></div></form></div>}</>}
