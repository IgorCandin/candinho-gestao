# SQLs manuais legados

Esta pasta preserva scripts SQL manuais de versões antigas que não fazem parte da sequência atual de migrations.

Não execute esses arquivos novamente sem comparar o script com o estado atual do banco e confirmar que a operação continua idempotente e necessária.
